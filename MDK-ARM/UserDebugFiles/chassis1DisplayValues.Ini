FUNC void displayvalues(void) {
	float i;
	
	exec("log > chassisValues.csv");
	printf("Ticks,M1 Speed,M1 Current\n");
	while(currTime == 0){
	}
	for (i = 0; i < 10000; i++){
		printf("%f,%.5f,%f\n", currTime, c1Output, c1SentPower);
	}
	exec("log off");
}