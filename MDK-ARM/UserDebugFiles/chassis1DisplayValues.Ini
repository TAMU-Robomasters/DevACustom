FUNC void displayvalues(void) {
	float i;
	float lastTime;

	exec("log > chassisValues.csv");
	printf("Ticks,M1 Speed,M1 Current,VelPID Derivative,Gimbal Loop Time,Yaw Data Process Time, CAN Loop Time, CAN Fill Level\n");
	while(currTime == 0){
	}
	while (currTime < 120000){
		if (currTime != lastTime){
			printf("%f,%f,%f,%f,%f,%f,%f,%f\n", currTime, c1Output, c1SentPower, c1Derivative, lastGimbLoopTime, lastYAWDataTime, lastCANLoopTime, canFillLevel);
			lastTime = currTime;
		}
	}
	exec("log off");
}