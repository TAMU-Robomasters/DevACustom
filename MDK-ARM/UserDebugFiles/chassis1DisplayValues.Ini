FUNC void displayvalues(void) {
	float i;
	float lastTime;

	exec("log > chassisValues.csv");
	printf("Ticks,M1 Speed,M1 Current,VelPID Derivative\n");
	while(currTime == 0){
	}
	while (currTime < 30000){
		if (currTime != lastTime){
			printf("%f,%f,%f,%f\n", currTime, c1Output, c1SentPower, c1Derivative);
			lastTime = currTime;
		}
	}
	exec("log off");
}